<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uni.ailab.scp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <provider
            android:name="com.uni.ailab.scp.provider.ScpProvider"
            android:authorities="com.uni.ailab.scp.provider"
            android:exported="true" >
        </provider>

        <!--
             <service
            android:name="com.uni.ailab.scp.service.ScpService"
            android:exported="true"
            tools:ignore="ExportedService" />
        -->
        <receiver android:name=".receiver.ScpPublicReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <action android:name="android.intent.action.PACKAGE_FULLY_REMOVED" />
                <action android:name="android.intent.action.PACKAGE_CHANGED" />

                <data android:scheme="package" />
            </intent-filter>
            <intent-filter>
                <action android:name="TESTSCP" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.uni.ailab.scp.MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <style>
    </style>

</manifest>